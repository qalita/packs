[project]
name = "numeric-validation-pack"
version = "0.2.0"
description = "Validates numeric data against configurable ranges and constraints"
authors = [
    {name = "QALITA SAS", email = "contact@qalita.io"}
]
license = {text = "Proprietary"}
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "qalita-core>=2.1.1",
    # Declared explicitly rather than inherited from qalita-core: this pack
    # calls polars directly and needs the streaming engine (>=1.25).
    "polars>=1.25.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
